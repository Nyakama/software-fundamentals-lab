# GitHub Workflows

This directory is reserved for future GitHub Actions workflows that check
formatting, linting, type safety, and tests.

Do not store credentials, tokens, private URLs, generated logs, or workflow
files that have not been reviewed here.

Future files might include `quality-checks.yml` or `tests.yml`.
