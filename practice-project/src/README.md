# Practice-Project Source

This directory is for future TypeScript source code belonging to the cohesive
practice project.

Do not store tests, generated build output, credentials, private assets, client
code, or unrelated exercises here.

Future files might include `index.ts` or `services/example-service.ts`.
