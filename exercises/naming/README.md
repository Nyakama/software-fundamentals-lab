# Naming Exercises

This directory is for small TypeScript exercises that practise clear variable,
function, type, and module names.

Do not store copied copyrighted exercises, answer keys, client code, or
unrelated project features here.

Future files might include `rename-variables.ts` or
`domain-language.test.ts`.
