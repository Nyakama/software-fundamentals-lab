# Legacy-Code Exercises

This directory is for deliberately constrained TypeScript examples and
characterisation tests used to practise working safely with legacy code.

Do not store code from real legacy systems, private repositories, clients, or
copyrighted training exercises here.

Future files might include `legacy-order-service.ts` or
`legacy-order-service.test.ts`.
