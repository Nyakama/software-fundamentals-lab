# Function Exercises

This directory is for focused TypeScript exercises involving function design,
responsibilities, parameters, and return values.

Do not store unrelated application features, copied solutions, or private
production code here.

Future files might include `extract-function.ts` or
`function-contracts.test.ts`.
