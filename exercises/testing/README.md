# Testing Exercises

This directory is for isolated testing exercises, test doubles, fixtures, and
examples of useful automated-test behaviour.

Do not store production data, credentials, client fixtures, or generated test
reports here.

Future files might include `calculator.test.ts` or
`fixtures/sanitised-user.ts`.
